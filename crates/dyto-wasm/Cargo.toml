[package]
name = "dyto-wasm"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dyto-scene = { path = "../dyto-scene", features = ["web"] }
wasm-bindgen = "0.2"
